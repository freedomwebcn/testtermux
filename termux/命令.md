
### 切换到笔记目录

``` shell
cd storage/shared/github
```

### 执行推送命令

``` shell
/data/data/com.termux/files/home/scripts/push-notes.sh
```

### 备注

> 由于手机系统限制，在termux设置的定时器任务只有在termux处于前台时才会自动执行。当termux处于后台时，即使定时器到了执行时机也不会执行的。

### 









